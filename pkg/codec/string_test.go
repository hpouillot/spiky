package codec

import (
	"testing"
)

func TestStringEncode(t *testing.T) {
	// codec := NewStringCodec(30, 10000000)
	// nodes := core.NewNodeSlice(10)
	// strToEncode := "Hello"
	// // err := codec.Encode(strToEncode, nodes)
	// if err != nil {
	// 	t.Error(err)
	// }
	// decodedStr, err := codec.Decode(nodes)
	// if err != nil {
	// 	t.Error(err)
	// }
	// if decodedStr != strToEncode {
	// 	fmt.Print(decodedStr)
	// 	t.Error("Invalid string")
	// }
}
